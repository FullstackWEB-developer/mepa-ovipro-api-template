// Constants for AWS SDK clients. You should not instantiate clients here, since this can be used from many places.

// Please consider having multiple separate namespaces if this gets big.

export namespace ClientConstants {
    export const DEFAULT_REGION = 'eu-west-1';
}
